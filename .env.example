# APP
APP_ENV=development
PORT=3333

# AUTH
WEBTOKEN_ENCRYPTION_KEY=uAsBw6WxqD
WEBTOKEN_EXPIRATION_TIME=2400

# DATABASE
DB_TYPE=mysql
DB_USERNAME=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=nest

# MCP
MCP_SERVER_NAME=nest-rest-typeorm-mcp
MCP_SERVER_VERSION=1.0.0
# Comma-separated list of external MCP server endpoints (Streamable HTTP) to connect to as a client.
# Example: https://mcp.example.com/sse,https://other.example.com/mcp
MCP_CLIENT_REMOTES=
